spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/order_master
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
