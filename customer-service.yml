spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:mysql://localhost:3306/customer_master
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
